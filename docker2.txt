ARG JAR_FILE=build/lib/transport-0.0.1-SNAPSHOT.jar transport-0.0.1-SNAPSHOT.jar
COPY ${JAR_FILE} app.jar
ENTRYPOINT ["java","-jar","/app.jar"]

docker build --build-arg JAR_FILE=build/libs/\*.jar -t springio/gs-spring-boot-docker .

docker build --build-arg JAR_FILE=build/libs/transport-0.0.1-SNAPSHOT.jar -t transport .

docker build -t transport/transport-0.0.1-SNAPSHOT .

docker build -t transport .

# Use an official OpenJDK runtime as a parent image
FROM openjdk:17-jdk-alpine

# Set the working directory in the container
#WORKDIR /app

# Copy the packaged jar file into the container
ADD build/lib/transport-0.0.1-SNAPSHOT.jar transport-0.0.1-SNAPSHOT.jar

# Expose the port the application runs on
EXPOSE 8080


# Run the jar file
ENTRYPOINT ["java", "-jar", "transport-0.0.1-SNAPSHOT.jar"]
